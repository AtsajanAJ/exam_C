#include<stdio.h>
int main()
{
    int n;
    printf("Enter n: ");
    scanf("%d", &n);
    
    printf("\n1. Pattern ปกติ (ซ้ายไปขวา):\n");
    for(int i = 1; i <= n; i++) {
        for(int j = 1; j <= i; j++) {
            printf("*");
        }
        printf("\n");
    }
    
    printf("\n2. Pattern กลับฝั่ง (ขวาไปซ้าย):\n");
    for(int i = 1; i <= n; i++) {
        // พิมพ์ช่องว่างก่อน
        for(int j = 1; j <= (n - i); j++) {
            printf(" ");
        }
        // พิมพ์ดาว
        for(int k = 1; k <= i; k++) {
            printf("*");
        }
        printf("\n");
    }
    
    printf("\n3. Pattern กลับหัว (บนลงล่าง):\n");
    for(int i = n; i >= 1; i--) {
        for(int j = 1; j <= i; j++) {
            printf("*");
        }
        printf("\n");
    }
    
    printf("\n4. Pattern กลับหัวและกลับฝั่ง:\n");
    for(int i = n; i >= 1; i--) {
        // พิมพ์ช่องว่างก่อน
        for(int j = 1; j <= (n - i); j++) {
            printf(" ");
        }
        // พิมพ์ดาว
        for(int k = 1; k <= i; k++) {
            printf("*");
        }
        printf("\n");
    }
    
    return 0;
}

/*
เคล็ดลับการจำง่ายๆ:
กลับฝั่ง (ซ้าย-ขวา):

เพิ่มช่องว่าง ก่อนพิมพ์ดาว
จำนวนช่องว่าง = (n - i)
ยิ่งแถวล่าง ช่องว่างยิ่งน้อย

กลับหัว (บน-ล่าง):

เปลี่ยน loop จาก i = 1 to n เป็น i = n to 1
ใช้ i-- แทน i++
แถวแรกจะมีดาวเยอะสุด แถวสุดท้ายมีดาวน้อยสุด

กลับทั้งคู่:

รวม ทั้งสองวิธี
ใช้ i = n to 1 + เพิ่มช่องว่าง

หลักการสำคัญ:

i = แถวปัจจุบัน
j = จำนวนช่องว่าง
k = จำนวนดาว

ลองรันโค้ดด้านบนดูครับ จะเห็นทั้ง 4 แบบในครั้งเดียว! 😊

*/